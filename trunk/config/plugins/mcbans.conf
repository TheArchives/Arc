[mcbans]
;apikey: XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
; Fill in the API key and remove the ; before it to use MCBans.
ban_threshold: 1
; Setting this to 0 will prevent MCBans from kicking anyone because of reputation, set this to kick anyone of a reputation less than you set.

[exceptions]
; Add exceptions here